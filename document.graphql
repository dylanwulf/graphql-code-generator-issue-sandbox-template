query cart {
    cartsById(id: "cart1") {
        id
        fulfillments {
            id
            lines {
                id
                item {
                    id
                }
            }
        }
    }
}
